{
  "name": "@whamp/implement-tickets",
  "version": "0.1.4",
  "private": true,
  "description": "Dependency-aware ticket implementation with independent implementation and review agents for pi-dynamic-workflows",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "npm test",
    "check:global": "node scripts/check.mjs",
    "install:global": "node scripts/install.mjs",
    "uninstall:global": "node scripts/uninstall.mjs"
  }
}
